# Task L2+L3 — Linux packaging, CI artifacts, smoke test, install docs

Repository: current working directory. Read `docs/plans/2026-09-03-capability-expansion.md` §2.8 and §4 "Block L / L2, L3", then `packaging/mouseshare.spec`, `packaging/entry.py`, `.github/workflows/build.yml` (all jobs), `mouseshare/__main__.py` (`smoke`, `_probe_backend`, `_probe_pynput`, `log_path`), `mouseshare/linux.py` (from L1), `docs/mac-install.md` (tone/format to mirror), `README.md` (Limits section and install sections), `pyproject.toml`. Test-first where there is Python to test; the CI workflow itself is verified by the controller pushing it (you cannot run GitHub Actions here), so keep it simple and copy the existing macOS/Windows job patterns exactly.

## Deliverables
1. **PyInstaller on Linux.** `packaging/mouseshare.spec`: on `sys.platform == "linux"` add hiddenimports `pynput.keyboard._xorg`, `pynput.mouse._xorg`, `Xlib.ext.xtest`, `Xlib.ext.record`, `webview.platforms.gtk`, and any `gi` overrides pywebview's GTK backend needs (`gi.overrides.Gtk`, `gi.overrides.WebKit2`); onefile binary named `mouseshare`; no `.ico` on Linux (avoid the PyInstaller warning). Bundle `packaging/icons/MouseShare.png` (generate 256 px from the existing `packaging/make_icons.py` if it does not already emit a PNG) for the desktop entry.
2. **`packaging/linux/`**: `mouseshare.desktop` (Name, Exec=mouseshare, Icon=mouseshare, Categories=Utility;), `build-appimage.sh` (downloads a pinned `appimagetool-x86_64.AppImage` by URL + sha256 into `dist/`, builds `MouseShare-x86_64.AppImage` from `dist/mouseshare` + desktop file + icon, `set -euo pipefail`), `nfpm.yaml` (name mouseshare, version taken from `pyproject.toml` at build time via `sed`, contents: `/usr/bin/mouseshare`, `/usr/share/applications/mouseshare.desktop`, `/usr/share/icons/hicolor/256x256/apps/mouseshare.png`; depends: `libgtk-3-0`, `libwebkit2gtk-4.1-0 | libwebkit2gtk-4.0-37`, `gir1.2-webkit2-4.1 | gir1.2-webkit2-4.0`, `python3-gi` is NOT needed since Python is bundled — but `gi` bundled by PyInstaller still needs the system typelibs, so depend on `gir1.2-gtk-3.0` and the webkit gir; rpm equivalents `gtk3`, `webkit2gtk4.1`), producing `.deb` and `.rpm` with `nfpm package -p deb|rpm` (pin nfpm version, install via the official GitHub release tarball with sha256).
3. **CI** (`.github/workflows/build.yml`): add `ubuntu-22.04` to the `package` matrix with: apt install of the GTK/WebKit dev + gir packages and `xvfb`, `pip install . pyinstaller`, `pyinstaller packaging/mouseshare.spec`, smoke `xvfb-run -a ./dist/mouseshare --smoke`, then `bash packaging/linux/build-appimage.sh`, nfpm deb + rpm, `tar -czf dist/MouseShare-linux-x86_64.tar.gz -C dist mouseshare` plus desktop file and icon; upload `MouseShare-linux` artifact containing the AppImage, `.deb`, `.rpm`, `.tar.gz`. Add a second smoke that runs the **AppImage** under xvfb (`./dist/MouseShare-x86_64.AppImage --smoke` with `APPIMAGE_EXTRACT_AND_RUN=1` because CI lacks FUSE). Extend the `release` job's `files:` list with the four Linux artifacts. Keep the existing macOS/Windows steps byte-identical.
4. **Smoke on Linux** must be meaningful: `_probe_backend` imports `webview.platforms.gtk` (from L1); `_probe_pynput` under xvfb must actually import the xorg backend and open the display; add a `_probe_linux_session` that prints the detected session type. `--smoke` exit code 1 on any failure (unchanged).
5. **Docs**: `docs/linux-install.md` mirroring `docs/mac-install.md`: which artifact to pick (AppImage vs deb vs rpm vs tarball), runtime dependencies per distro (Ubuntu 22.04/24.04, Fedora 40+), the X11 requirement and how to pick "Ubuntu on Xorg" at login, Wayland explicitly unsupported with the reason (compositor forbids global capture/injection; portal path is future work), firewall (`ufw allow 5353/udp` and the app port; firewalld equivalents), config/log locations (XDG paths from L1), troubleshooting (blank window → missing WebKitGTK; no capture → Wayland session; permission denied on `/dev/uinput` not needed on X11). README: replace "no Linux build" in Limits with a pointer to the doc and the X11-only statement; add Linux to the install section.
6. `docs/plans/...`: tick L2/L3 in Progress with one line each.

### Tests
- `tests/test_main_smoke.py` (or extend an existing test module for `__main__`): with fake modules, the Linux smoke probes pass/fail correctly; `log_path`/config dir on Linux resolve to XDG locations.
- `packaging/linux/build-appimage.sh` and `nfpm.yaml` get a syntax check in the check command (`bash -n`, and `python3 -c "import yaml"`-free validation via a tiny grep for required keys).

### Constraints
- No new Python dependencies. Nothing distribution-specific in `mouseshare/`. Do not commit generated artifacts. Preservation rules apply. Do NOT commit.

## Check command (must exit 0)
```
ruff check mouseshare tests packaging tools && python3 -m pytest -q tests && bash -n packaging/linux/build-appimage.sh && grep -q "^name: mouseshare" packaging/linux/nfpm.yaml
```

## Report
Files changed, the exact CI steps added (as a list), artifact names, check tail (2 lines), anything left out with reason (note explicitly that CI has not been run).
