# Task H2 — add ruff linting to MouseShare

Repository: the current working directory (Python 3.12+, package `mouseshare/`, tests in `tests/`, CI in `.github/workflows/build.yml`). Read `docs/plans/2026-09-03-capability-expansion.md` §4 "Block H" for context; implement only H2.

## Requirements
1. Add a `[tool.ruff]` section to `pyproject.toml`: `target-version = "py312"`, `line-length = 100`, default rule set plus `I` (import sorting). Exclude `packaging/` build output and `docs/`.
2. Add a `lint` job (or step in the existing `test` job) in `.github/workflows/build.yml` that runs `pip install ruff` and `ruff check mouseshare tests packaging tools`. The `package` job must still depend on tests passing.
3. Run `ruff check mouseshare tests packaging tools` locally and fix every finding **mechanically** (unused imports, import order, obvious unused variables). Do not refactor, rename, reformat beyond what ruff demands, or change behaviour. If a finding would require a behavioural change, add a targeted `# noqa: <code>` with a one-line reason instead.
4. Do not run `ruff format`. Do not touch `mouseshare/ui/web/`.
5. `python3 -m pytest -q tests` must still pass (167 tests).
6. Add `ruff` to a `[project.optional-dependencies] dev` list alongside `pytest`.
7. Commit with a focused message: "Lint with ruff" — one commit, only the files this task needs. Do not commit anything else that may be in the working tree.

## Check command (must exit 0)
```
ruff check mouseshare tests packaging tools && python3 -m pytest -q tests
```
Install ruff with `pip install --user ruff` (or `pipx`) if missing.

## Report
Finish with a short summary: files changed, number of ruff findings fixed, any `noqa` added and why, and the exact check-command output tail (5 lines).
