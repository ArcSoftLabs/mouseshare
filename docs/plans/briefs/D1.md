# Task D1 — user documentation, protocol doc, support matrix, release notes

Repository: current working directory. Read `docs/plans/2026-09-03-capability-expansion.md` (all, especially §3 "Verification reality", the Progress list, and the decisions), `README.md`, `docs/mac-install.md`, `docs/linux-install.md`, `docs/file-transfer.md`, `mouseshare/protocol.py` (every message builder and the version rules), `mouseshare/clipboard.py`, `mouseshare/transfer.py` (if present), `mouseshare/capture.py` (escape gesture), and the verification notes the controller will paste below. Documentation only: do not change code or tests.

## Deliverables
1. **`docs/protocol.md`**: framing, versions 2 and 3, first-frame negotiation (`max_v`), the pin rule, capabilities (`heartbeat`, `clipboard`, `files`), every message type with fields and direction, optional-type semantics, chunking and caps, pairing/auth flow including the `pair_ok` HMAC (inputs, no secrets), refusal reasons (`busy`, `duplicate`, `full`, `disabled`, …), heartbeat timings, and what a v2 peer gets (input only, unauthenticated-peer warning).
2. **`docs/support-matrix.md`**: three columns — **Verified** (a real run happened; cite date and how), **Implementation-only** (code + tests exist, no real run), **Unsupported** (with reason). Rows: every pairing in the acceptance matrix (Windows↔Windows, Windows↔macOS both directions, Windows↔Linux, macOS↔macOS, macOS↔Linux, Linux↔Linux) × features (connection/reconnection, keyboard, pointer, boundary crossing both ways, clipboard, file transfer, mixed DPI, recovery after peer termination), plus 4-device sessions, protocol v2 interop, and Wayland. Use ONLY the evidence in the notes below; everything else is implementation-only. Do not upgrade anything to Verified on your own.
3. **README.md**: update features (multi-device up to 8 clients, escape gesture, clipboard, file transfer, Linux X11), install section pointing to the three platform docs, a "Known limitations" section (Wayland, mixed DPI, macOS multi-display origin if still pending, native drag-and-drop status per `docs/file-transfer.md`), and the escape gesture explanation.
4. **`docs/release-notes-0.3.0.md`**: user-facing summary of everything since v0.2.0 (use `git log v0.2.0..HEAD --oneline` for the list), the compatibility statement (v0.3.0 pairs with v0.2.0 for input only), and the upgrade note (config migrates automatically; Linux config moves to XDG paths).
5. Bump `version` in `pyproject.toml` to `0.3.0` only if the controller's notes say so; otherwise leave it.

## Evidence notes (paste from the controller — do not invent)
Date of all evidence: 2026-09-04. Controller: Fable (Claude). Only the items marked VERIFIED may go in the Verified column.

VERIFIED (a real run happened):
1. Protocol v2 interop, Windows host (this tree, at commit 26e0f36 "P4") <-> macOS client running the packaged v0.2.0 app: live run with `tools/live_v2_probe.py` on Windows Python 3.14. Result: `phase=session role=host connected=True negotiated_version=2`; sent pair_request/auth/layout at v2; received pair_challenge/pair_ok at v2; the client was flagged `unauthenticated_peer: True` (v2 peers cannot do the mutual pair_ok HMAC). Pairing over token only; NO input, clipboard, or file traffic was exercised. Support-matrix wording: "pairing verified live; input untested".
2. Linux X11 backend on WSLg (XWayland), `tools/linux_x11_smoke.py`: session detection (session_type=x11, no extra permissions), xorg listeners and warper alive, start_remote grabbed pointer+keyboard (GrabSuccess), warp to the anchor confirmed by query_pointer, stop_remote ungrabbed cleanly. No human input was exercised, no second device was connected. Support-matrix wording: "grab/warp/ungrab verified on WSLg; no peer, no human input".
3. Windows (Python 3.14 via py.exe): the full unit suite passes on real Windows Python (355 passed, 1 skipped at HEAD); DPI coordinate-space consistency was measured on the PC (the app's virtual-screen coordinates match what the hooks report); the native drop-target window in `tools/dragdrop/win_edge_drop.py` registers successfully (no real drag was performed).

NOT verified (say so explicitly where relevant):
- Windows clipboard backend: a probe from a WSL-launched process got ERROR_ACCESS_DENIED from OpenClipboard while the interactive session was disconnected; PowerShell Get-Clipboard failed identically, so this is environmental. Clipboard is implementation-only on every platform.
- File transfer: implementation-only on every platform (loopback tests only, both interpreters).
- Native cross-boundary drag-and-drop (F0 spikes): awaiting Benjamin's runs on Windows and macOS; see `docs/file-transfer.md`. The shipped trigger in 0.3.0 is the in-window drop zone / file picker ("Send files"). Document native edge-drop as "under evaluation".
- macOS capture rework (tasks A2/A3/A4) has not been done; the macOS backend is the pre-existing one plus the escape gesture. macOS multi-display origin is a known pending item.
- macOS 0.3.0 has never been run at all (only the v0.2.0 packaged app on the Mac mini).
- Linux packaging (AppImage/deb/rpm/tarball) and the GitHub Actions workflow: implementation-only; CI has never run because nothing has been pushed.
- 4-device sessions, reconnection, recovery after peer termination, mixed DPI across machines: implementation-only (loopback/unit tests).

UNSUPPORTED by decision: Wayland (X11 only; state the reason: no global input grab/warp on Wayland without a compositor-specific portal).

Version: do NOT bump `pyproject.toml`; the controller bumps at tagging time.

Preservation rules: edit README.md incrementally (keep its existing sections, badges, and tone; add/adjust, never rewrite wholesale). Do not touch code or tests. Do NOT commit. Do not modify anything under `docs/plans/`.

## Check command (must exit 0)
```
ruff check mouseshare tests && python3 -m pytest -q tests && test -s docs/protocol.md && test -s docs/support-matrix.md && test -s docs/release-notes-0.3.0.md
```

## Report
Files changed, the count of Verified / Implementation-only / Unsupported cells, and anything you could not document because evidence was missing.
