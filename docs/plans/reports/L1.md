Implemented L1 without committing.

Files changed:

- `mouseshare/linux.py` — new, 0 → 90 lines
- `mouseshare/capture.py` — 372 → 436
- `mouseshare/inject.py` — 117 → 137
- `mouseshare/config.py` — 126 → 143
- `mouseshare/__main__.py` — 199 → 215
- `mouseshare/app.py` — 1057 → 1072
- `tests/test_linux.py` — new, 0 → 204
- `docs/plans/2026-09-03-capability-expansion.md` — L1 ticked

No module shrank. Existing Windows/macOS tests and assertions were untouched.

X11 remote-mode mechanism, in 5 lines:

1. Linux starts pynput’s explicit Xorg listeners without `suppress=True`.
2. `start_remote()` grabs the root pointer and keyboard with `owner_events=False`.
3. Motion callbacks calculate `position - anchor` using the existing limit rules.
4. Valid motion queues an anchor warp on a dedicated worker thread.
5. Anchor-generated events are ignored, and `stop_remote()` releases both grabs.

Wayland is detected before pynput loads. Capture raises a clear X11-required `RuntimeError`; permissions report `reason: "wayland"`, and `open_permissions()` returns the requested “Ubuntu on Xorg” explanation. Missing displays report `reason: "no display"`.

XDG paths:

- Config: `$XDG_CONFIG_HOME/mouseshare/config.json`, defaulting to `~/.config/mouseshare/config.json`
- Log: `$XDG_STATE_HOME/mouseshare/debug.log`, defaulting to `~/.local/state/mouseshare/debug.log`
- Legacy `~/.mouseshare/config.json` is copied forward once when needed and retained.

Check tail:

```text
All checks passed!
288 passed in 19.66s
```

Windows Python also passed all 288 tests. Nothing was left out; `monitors.py` already had the required exception-to-fallback behavior, so it needed no change.