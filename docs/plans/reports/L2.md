Implemented L2+L3 without committing.

Files changed:

- `.github/workflows/build.yml`
- `README.md`
- `docs/linux-install.md`
- `docs/plans/2026-09-03-capability-expansion.md`
- `mouseshare/__main__.py`
- `packaging/make_icons.py`
- `packaging/mouseshare.spec`
- `packaging/icons/MouseShare.png`
- `packaging/linux/build-appimage.sh`
- `packaging/linux/mouseshare.desktop`
- `packaging/linux/nfpm.yaml`
- `tests/test_main_smoke.py`

CI additions:

- Added `ubuntu-22.04` to the package matrix.
- Installed GTK3, WebKitGTK, GIR, Cairo development packages, and Xvfb.
- Installed pinned `PyGObject==3.46.0` for PyInstaller bundling.
- Built with `pyinstaller packaging/mouseshare.spec`.
- Ran `xvfb-run -a ./dist/mouseshare --smoke`.
- Built the AppImage with `bash packaging/linux/build-appimage.sh`.
- Ran the AppImage smoke test with `APPIMAGE_EXTRACT_AND_RUN=1`.
- Downloaded and checksum-verified nFPM 2.47.0.
- Extracted the version from `pyproject.toml` using `sed`.
- Built versioned `.deb` and `.rpm` packages.
- Built the Linux tarball containing the executable, desktop file, and icon.
- Uploaded the four Linux files as `MouseShare-linux`.
- Added all four Linux outputs to tagged releases.

Artifacts:

- `MouseShare-x86_64.AppImage`
- `mouseshare_0.2.0_amd64.deb`
- `mouseshare-0.2.0-1.x86_64.rpm`
- `MouseShare-linux-x86_64.tar.gz`

Check tail:

```text
...............                                                          [100%]
303 passed in 21.22s
```

The nFPM configuration was additionally exercised with disposable `.deb` and `.rpm` builds. GitHub Actions and the complete frozen Linux/AppImage build have not been run locally; the controller must verify those by pushing the workflow. No generated release artifacts were added, and no commit was made.