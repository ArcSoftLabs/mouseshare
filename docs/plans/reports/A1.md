Implemented Task A1 without committing.

Files changed:

- `mouseshare/{capture,session,app,config}.py`
- `mouseshare/ui/web/{index.html,app.js}`
- `tests/{test_escape,test_session,test_capture_windows,test_config,test_app}.py`
- `docs/plans/2026-09-03-capability-expansion.md` — ticked A1 only

Escape detection:

- Windows: inside the low-level keyboard filter, after pynput decodes the raw event.
- macOS: inside the decoded keyboard callbacks, with the corresponding event consumed by the Darwin intercept.
- Supports left/right `ctrl`, `cmd`, `alt`, and `shift`; changes apply immediately.

Re-warp rule: normal edge returns use the mapped/inset local point; escape uses the local monitor point nearest the modeled peer position; disconnect/capture loss falls back to the inset anchor-monitor center. Release remains idempotent.

Watchdog design:

- One daemon thread polls every 0.5 seconds while capture is started.
- It observes both listener `running` state and exceptions re-raised by `join(timeout=0)`.
- Listener death clears suppression and calls `on_capture_lost()`, which releases, sends `leave`, and re-warps.

Check-command tail:

```text
All checks passed!
........................................................................ [ 36%]
........................................................................ [ 73%]
....................................................                     [100%]
196 passed in 12.14s
```

Deliberately left out: protocol changes and the macOS capture root-cause rewrite, because those belong to P/A2–A3. No live macOS validation was possible in this environment.