Implemented P4 test-first; no commit created. The [TDD skill](/home/benjaminh-dev/.agents/skills/tdd/SKILL.md) guided the red/green cycles.

Files changed (before → after lines):

- `mouseshare/network.py`: 335 → 351
- `mouseshare/app.py`: 1050 → 1057
- `tests/test_network.py`: 270 → 337
- `tests/test_app.py`: 921 → 1007
- `tests/test_security.py`: 388 → 411
- `docs/plans/2026-09-03-capability-expansion.md`: 331 → 333
- `protocol.py` and `test_protocol.py`: unchanged at 221 and 135

Negotiation:

1. Connector sends its first `pair_request` as `v:2` with `max_v:3`.
2. Responder pins `min(VERSION, max_v or v)` and replies using that version.
3. Connector pins the reply’s `v`; subsequent outbound frames use it.
4. Later inbound version changes remain fatal; v2 sessions retain unauthenticated-warning semantics.

The app-level downgrade test was mutation-checked: removing the pin made it fail as expected. Added coverage also confirms strict-v2 token/code pairing, authenticated v3 `pair_ok`, and responder teardown on v2 `auth` after negotiating v3.

Check tail:

```text
..........................................................               [100%]
274 passed in 19.21s
```