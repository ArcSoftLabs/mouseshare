Implemented all requested review fixes without committing:

- Gate 1: added a gated-worker test proving queued keys are dropped during teardown (`tests/test_app.py:185`).
- Critical teardown deadlock: split out idempotent `stop_inbound()` and invoke it outside the app lock (`mouseshare/network.py:101`, `mouseshare/app.py:984`). Added timing/reason-preservation coverage (`tests/test_app.py:222`).
- Outbox ordering: restored flush before socket closure and tested delivery of a final `leave` (`mouseshare/app.py:999`, `tests/test_app.py:256`).
- Reader leak: balanced `task_done()` and verified the reader exits within one second (`mouseshare/network.py:83`, `tests/test_network.py:210`).
- Flood assertion: now checks all keys arrive and received positions remain monotonic and bounded (`tests/test_robustness.py:105`).
- Heartbeat gate: added assertions immediately after the first idle call (`tests/test_app.py:398`).

Measured teardown time: **0.020509 s**.

Check-command tail:

```text
...................................................................      [100%]
211 passed in 14.45s
```