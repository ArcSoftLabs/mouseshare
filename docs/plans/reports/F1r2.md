Implemented all six requested deltas without committing.

1. Sender cancellation after waits — [transfer.py](/mnt/c/Users/A508639/Documents/Claude%20Projects/mouseshare/mouseshare/transfer.py:351)

   Added explicit cancelled/failed checks after acceptance and ACK waits and before `xfer_done`/final `done`. Added `test_receiver_cancel_on_last_chunk_cancels_both_without_done`.

2. Receive cancellation race — [transfer.py](/mnt/c/Users/A508639/Documents/Claude%20Projects/mouseshare/mouseshare/transfer.py:237)

   Locked lookup through decode/write and locked the done/verification path. Cancellation-triggered `ValueError` exits quietly, with sends outside the lock. Added `test_receiver_cancel_racing_chunk_keeps_link_up`.

3. Position coalescing test — [test_transfer.py](/mnt/c/Users/A508639/Documents/Claude%20Projects/mouseshare/tests/test_transfer.py:363)

   `test_position_traffic_remains_ordered_during_multichunk_transfer` now verifies strictly increasing positions, arrival of `x=199` within one second, and traffic during an active transfer.

4. Peak memory test — [test_transfer.py](/mnt/c/Users/A508639/Documents/Claude%20Projects/mouseshare/tests/test_transfer.py:156)

   `test_one_mib_transfer_streams_with_bounded_sender_and_receiver_memory` now uses `reset_peak()` and enforces a combined peak below 512 KiB. The ACK window was reduced from eight chunks to four at [transfer.py](/mnt/c/Users/A508639/Documents/Claude%20Projects/mouseshare/mouseshare/transfer.py:23).

5. Competing-offer cleanup — [test_transfer.py](/mnt/c/Users/A508639/Documents/Claude%20Projects/mouseshare/tests/test_transfer.py:468)

   `test_two_offers_sharing_later_name_do_not_unlink_winners_part` now cancels the loser, checks the winner’s reservation and receive state, then completes and verifies both files.

6. Dead condition and Windows note — [transfer.py](/mnt/c/Users/A508639/Documents/Claude%20Projects/mouseshare/mouseshare/transfer.py:237)

   Changed chunk-count validation ordering so the changed-count branch is primary after the first chunk. Added the POSIX-only fallback note at [transfer.py](/mnt/c/Users/A508639/Documents/Claude%20Projects/mouseshare/mouseshare/transfer.py:322). Existing `test_wrong_or_changed_chunk_count_is_rejected` remains intact.

Line counts:

- `transfer.py`: 427 → 451
- `test_transfer.py`: 450 → 519

The exact check command passed three consecutive times. Third run tail:

```text
....................................................................     [100%]
356 passed in 27.11s
```