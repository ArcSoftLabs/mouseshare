# Handoff — MouseShare capability expansion (2026-09-04, ~10:50)

Read this first in a fresh session. The full plan is
`docs/plans/2026-09-03-capability-expansion.md`; briefs for every task are in
`docs/plans/briefs/`, and Sol's reports (useful for delta reviews) in
`docs/plans/reports/`.

## Where we are

`main` is at **bf846d1** (16 commits since 0ca7e2c), all reviewed by the
Sol-implements / Fable-reviews loop (two gates + bounded delta reviews). Every
commit passes `ruff check mouseshare tests` and the suite on **both** Linux
(`python3 -m pytest -q tests`) and Windows (`py.exe -m pytest -q tests`).

| Task | Status |
|---|---|
| H1/H2 housekeeping, ruff | committed |
| P1 protocol v3 + heartbeat | committed |
| A1 escape gesture (double-tap Ctrl/Cmd), re-warp, watchdog | committed |
| P2 client send path, inbound queue | committed |
| P3 mutual `pair_ok` auth | committed |
| M-A / M-B multi-device (8 clients, routing, editor) | committed |
| C clipboard | committed |
| P4 first-frame negotiation (v0.2.0 interop) | committed, **verified live** vs the Mac's v0.2.0 |
| L1 Linux X11 backend | committed, grab/warp/ungrab **verified live** on WSLg |
| L2/L3 Linux packaging, CI, docs | committed, **CI never run** (nothing pushed) |
| F0 drag-and-drop spikes | committed; Windows spike registers OK, needs Benjamin's drag |
| **F1 file transfer** | **UNCOMMITTED in the working tree**, gate 1 FAILED — see below |
| F2 native edge-drop wiring | not started; depends on F0 results |
| A0 Mac probe → A2/A3/A4 macOS capture fix | not started; depends on Benjamin running `tools/mac_tap_probe.py` on the Mac |
| D1 docs/support matrix/release notes, D2 final review | not started; brief `docs/plans/briefs/D1.md` |

## The uncommitted F1 tree

`git status` shows F1's first round: `mouseshare/transfer.py`, `tests/test_transfer.py`
(new), plus edits to `app.py`, `protocol.py`, `config.py`, `ui/web/*`, `tests/test_app.py`,
the plan doc. 327 tests pass, but the review (`docs/plans/reports/F1.md` is Sol's report;
the reviewer's findings are reproduced verbatim in `docs/plans/briefs/F1-rework1.md`)
found a **critical** (bytes past the declared size are never bounded), overwrite-on-rename,
multi-file `.part` races, a setting-toggle hang, and **eleven missing tests**. Next step:
run Sol on `F1-rework1.md`, then a delta review, then commit as one "file transfer" commit.
Do not commit the tree as it stands.

## How Sol is run (and the problem to route around)

Working invocation (see memory `codex-sol-runner`):

```
cd "<repo>"; export PATH=$HOME/.local/bin:$PATH
codex exec --model gpt-5.6-sol -s workspace-write \
  -c 'sandbox_workspace_write.network_access=true' --skip-git-repo-check \
  -o <out>/<task>.last.md "$(cat docs/plans/briefs/<task>.md)" < /dev/null > <out>/<task>.log 2>&1
```

**Problem:** in the last session, background Bash jobs (and a Monitor) were killed
from outside the session five times — three Codex runs died within a minute of launch,
tree untouched each time. Not Benjamin. **Different route for the next session:**
launch Codex fully detached so the harness cannot kill it, then check the output file:

```
setsid nohup bash -c 'cd "<repo>" && export PATH=$HOME/.local/bin:$PATH && codex exec ... ' \
  > /dev/null 2>&1 < /dev/null &
```

and detect completion by the existence/size of `<task>.last.md` (a Bash `until` loop in a
*foreground* call bounded at 9 minutes, repeated, or ask Benjamin to say "check"). If
detaching still gets killed, run `codex exec` in the **foreground** with `timeout 590`
per call and use `codex exec --resume-last`/`resume` semantics (`codex exec resume --last`)
to continue a task across calls.

Reviewer subagents (Agent tool, fresh context, read-only) were never killed; keep
using them. Review protocol and budget: gate 1 + gate 2, at most two rework rounds, then
the controller decides (Benjamin's instruction: converge fast). Every brief carries a
"Preservation rules" block because Sol once rewrote `app.py` wholesale (memory
`sol-rewrites-need-preservation-rules`); always check `git diff --stat` for net shrink.

## Live-verification assets

- Mac mini: `ssh -i ~/.ssh/mouseshare_mac bh@192.168.129.86` (memory `mac-mini-ssh`). Probes
  are in `~/mouseshare-probe/` there (`mac_tap_probe.py`, `mac_edge_drop.py`, `check.py`);
  pyobjc installed user-level. SSH processes are **not** Accessibility-trusted; Benjamin must
  run the probes in Terminal. The packaged v0.2.0 app is running there.
- Windows: `py.exe` is Windows Python 3.14; `tools/live_v2_probe.py` (needs a copy of
  `C:\Users\A508639\.mouseshare\config.json`; run `py.exe tools/live_v2_probe.py`),
  `tools/win_clipboard_probe.py` (clipboard access was denied for WSL-launched processes
  while the session was disconnected — re-run with Benjamin at the PC),
  `tools/dragdrop/win_edge_drop.py` (registers fine; needs a real drag).
- WSLg: `tools/linux_x11_smoke.py` (X11 grab/warp/ungrab, passes).
- A git worktree at `/tmp/claude-1000/ms-live` (HEAD 4b559a4) may still exist; remove
  with `git worktree remove --force /tmp/claude-1000/ms-live` if present.

## Evidence for the support matrix (only these are "verified")

1. Windows-as-host ↔ v0.2.0 macOS client: pairing over token, protocol negotiated to 2 (live, 2026-09-04).
2. Linux X11 backend on WSLg/XWayland: session detection, grabs, warp, ungrab (live).
3. Windows: DPI space consistency measured; drop-target registration works; unit suite on real Windows Python.
Everything else is implementation-only until run. Wayland unsupported by decision.

## Decisions (Benjamin, 2026-09-03)

Sol implements, Fable plans/reviews; real cross-boundary drag-and-drop is the target
(drop-zone fallback only where F0 proves impossibility); 8 clients max; Linux X11 only;
escape = double-tap Ctrl (Cmd on Mac), configurable. Nothing pushed yet — ask before pushing.

## Remaining order

F1 rework → commit → (F2 if F0 evidence) → A0 result → A2/A3/A4 → D1 docs → final
integration review (`docs/plans/briefs/FINAL-REVIEW-NOTES.md` lists the controller-edited
hunks that no reviewer has seen) → push (with permission) → CI → support matrix filled
from real runs → tag v0.3.0.
